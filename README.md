Prerequis :
    Xampp/Wamp/Mamp(avec php 8.1 ou une version superieure et mysql 5.7.7 ou une version superieure )
    

1ere etape : Installer et configurer Laravel 
    1-Telecharger Installer composer:
        - telecharger l' executable via ce site "getcomposer.org/download"
        - l'executer .S'il ne detecte pas automatiquement le chemin vers le fichier php.exe, preciser le chemin manuellement(exemple C:\Wamp64\bin\php\phpX.X.X\php.exe)
        - verifier l' installation en tapant ' composer --version '
     